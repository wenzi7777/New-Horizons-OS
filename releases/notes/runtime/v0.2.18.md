# New Horizons Runtime v0.2.18

- Add native `vdboard.scan` streaming APIs for matrix packet assembly, filter state, calibration interpolation, HMAC, and stream memory stats.
- Preserve the 8MB flash / no-PSRAM runtime target for VD-CTL/R v1.0.F 2026.4.
- Runtime/base firmware is still manually flashed; OS and Recovery remain separately versioned.
