# New Horizons Runtime v0.2.19

- Compact native stream calibration memory so full matrix layouts do not reserve per-cell calibration slots up front.
- Keep the existing binary packet and CSV contracts unchanged.
- Rebuild the 8MB/no-PSRAM base firmware with the updated native `vdboard.scan` module.

