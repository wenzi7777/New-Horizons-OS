# New Horizons Recovery v0.2.22

- Remove the legacy UDP control server from recovery mode.
- Make MQTT the only command and status transport in recovery.
- Remove legacy master/data server runtime fields and publish a manifest delete entry for the old UDP control file.

