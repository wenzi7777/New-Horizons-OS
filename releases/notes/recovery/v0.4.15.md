# New Horizons Recovery v0.4.15

- Opens setup AP when `boot_request=wifi_setup`, even if the root launcher only entered Recovery by mode.
- Falls back to setup AP when saved Wi-Fi credentials associate but DHCP does not complete.
- Extends DHCP-pending observation before reconnect attempts to reduce STA retry storms.
