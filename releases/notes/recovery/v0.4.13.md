# New Horizons Recovery v0.4.13

- Add the same link-no-IP Wi-Fi recovery state used by the normal OS.
- Reject expired and wrong-mode boot commands before scheduling reboot.
- Include Wi-Fi diagnostics in status and memory status responses.
- Keep Recovery `write_os` compatible with `latest.json` so older Recovery builds can install the v0.4.13 OS.
