# New Horizons Recovery v0.3.0

- Report the full 6-byte device UID in recovery status and command results.
- Align recovery update metadata with OS and Runtime v0.3.0.
- Keep TCP JSONL control and HTTP manifest OTA flow.

Requires New Horizons Runtime v0.3.0 for full MAC UDP visualization after entering OS.
