# New Horizons Recovery v0.4.14

- Allow Recovery `write_os` and `check_os_release` to use an explicit request release URL.
- Fall back to runtime `update.release_url`, then the bundled default release URL.
- Keep the default path on GitHub `latest.json` for normal WebUI upgrades.
