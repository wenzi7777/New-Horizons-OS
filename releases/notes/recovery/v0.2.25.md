# New Horizons Recovery v0.2.25

- Fix `write_os` ENOMEM during OTA by releasing Wi-Fi setup portal memory before update writes.
- Load `os_writer` lazily only when checking or writing OS releases.
- Stream the OS update plan one file at a time instead of holding the full download plan in recovery heap.

