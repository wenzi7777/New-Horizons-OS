# New Horizons Recovery v0.4.2

- Fix TCP control sends on ESP32 nonblocking sockets by retrying `EAGAIN` / would-block writes instead of closing the FindMe attachment.
- Support partial TCP writes for recovery hello, status, command result, and OS write progress messages.
- Keep Runtime at v0.3.0.

Requires New Horizons Runtime v0.3.0.
