# New Horizons Recovery v0.2.28

- Replace recovery MQTT command/status handling with the same TCP JSONL control session used by OS.
- Report `hello`, `status`, command `result`, and `update_progress` over TCP for more immediate WebUI update progress.
- Keep OTA file download through the existing HTTP manifest writer; TCP is used only for command and progress control.
- Update Wi-Fi setup to configure New Horizons server host, TCP control port, and UDP data port instead of MQTT broker fields.
- Delete old `mqtt_transport.py` and `umqtt` files from recovery during update.
