# New Horizons OS v0.6.6

## Changes

- Added OLED screen rotation setting. The external SSD1306 display now exposes a `rotation` field (0–3) in `OledConfig`, settable via the `set_indicators` UDP command. The value maps directly to `Adafruit_SSD1306::setRotation()`: 0 = normal, 1 = 90° clockwise, 2 = 180°, 3 = 270° clockwise. The rotation is persisted to `/config/device.json` and restored on boot. Default is 0 (no rotation). Desktop app v0.1.6+ exposes a dropdown in the screen settings panel.
