# New Horizons OS v0.2.24

- Roll back `set_matrix_layout` if scan startup fails, so failed full-matrix allocation does not persist a bad layout.
- Lower normal streaming GC cadence to reduce MicroPython heap buildup while publishing larger matrix packets.
- Keep matrix size, FPS, Wi-Fi buffers, packet format, and CSV output unchanged.

