# New Horizons OS v0.4.23

- Optimized BMI270 startup memory behavior after serial profiling showed cold `load_config_file()` temporary allocations recover after GC.
- Collects immediately after the upstream BMI270 constructor streams the 8 KiB config blob.
- Avoids the duplicate `load_config_file()` call when the driver already reports `internal_status == 0x01`.
