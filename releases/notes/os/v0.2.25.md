# New Horizons OS v0.2.25

- Use a streamed update plan for recovery updates instead of keeping the full download list in RAM.
- Release temporary download references aggressively after each file.
- Keep runtime/base firmware at `v0.2.19`.

