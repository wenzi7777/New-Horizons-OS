# New Horizons OS v0.4.22

- Fixed the v0.4.21 boot wrapper to avoid CPython-only `object.__getattribute__` calls that are not available in MicroPython.
- Keeps the lightweight Wi-Fi preconnect before loading `app_core.py`.
