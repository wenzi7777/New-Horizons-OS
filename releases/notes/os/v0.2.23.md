# New Horizons OS v0.2.23

- Move the normal streaming hot path to the native `vdboard.scan` packet pipeline.
- Keep the existing MQTT binary packet and server CSV output unchanged.
- Lazy-load maintenance modules so normal mode keeps RAM available for matrix scanning.
- Add native stream and memory stats to status output for full matrix diagnostics.
- Remove the legacy calibration-mode command flow; calibration now runs through maintenance commands only.
