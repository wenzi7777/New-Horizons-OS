# New Horizons OS v0.4.7

- Recovery update low-memory mode now releases UDP streaming, time sync, IMU, battery, LED, scan, packet, and related modules before writing Recovery.
- `check_recovery_release` also enters the same low-memory preparation when already in maintenance mode.
- OTA manifests are now compact JSON to reduce transient allocation pressure on MicroPython.
