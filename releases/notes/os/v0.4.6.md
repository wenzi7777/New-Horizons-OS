# New Horizons OS v0.4.6

- Recovery updates now require maintenance mode before writing, so normal matrix scanning is stopped and heap is freed before the Recovery writer runs.
- The WebUI Recovery update action now enters maintenance automatically before sending `write_recovery`.
- `write_recovery` now reports `maintenance_required` instead of attempting the write from a memory-tight normal streaming session.
