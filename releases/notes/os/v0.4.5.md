# New Horizons OS v0.4.5

- Stream recovery update progress as compact `update_progress` messages and flush them during the long-running write command.
- Report recovery write failures as structured error `update_state` instead of leaving the WebUI waiting for a timeout.
- Keep Runtime at v0.3.0.

Recovery v0.4.5 is recommended before running OS writes from Recovery.
