# New Horizons OS v0.4.10

- Supersedes v0.4.9 with OS and Recovery manifest URLs aligned to the same release tag.
- Reduced gateway reconnect resource pressure with TCP exponential reconnect backoff and gateway-lost state reporting.
- FindMe rediscovery now treats gateway timeout/lost TCP states as retryable, so devices can recover after Gateway restarts without high-frequency retry loops.

