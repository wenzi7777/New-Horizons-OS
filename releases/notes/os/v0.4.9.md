# New Horizons OS v0.4.9

- Reduced gateway reconnect resource pressure with a longer TCP timeout window, exponential reconnect backoff, and gateway-lost state reporting.
- FindMe rediscovery now treats gateway timeout/lost TCP states as retryable, so devices can recover after Gateway restarts without high-frequency retry loops.
- OS status now points Recovery updates to Recovery v0.4.8.

