# New Horizons OS v0.4.26

- Clears boot/recovery shared modules before normal OS bootstrap so UDP control uses `/nhos` modules after `write_os`.
- Keeps `status_announce` below a single UDP control packet budget; detailed diagnostics remain available through explicit commands.
- Keeps Recovery at `v0.4.25`.
