# New Horizons OS v0.2.22

- Remove the legacy UDP control server and UDP sensor data stream from normal OS.
- Make MQTT the only transport path for commands, status, and raw sensor packets.
- Remove legacy master/data server runtime fields and publish manifest delete entries for old UDP files.

