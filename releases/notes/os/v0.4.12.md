# New Horizons OS v0.4.12

- Fix normal OS Wi-Fi startup after Recovery `write_os` by adding a DHCP grace window when STA is already associating but has not received an IP yet.
- Avoid resetting the STA interface during that grace window, reducing reconnect churn on multi-AP networks.
- Publish matching OS and Recovery manifests for the slow-DHCP Wi-Fi fix.
